<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="alternate" type="application/rss+xml" title="Waxlimbs" href="/feed.rss">
    <link rel="stylesheet" href="/assets/styles.css">
    <title>{{ site.title }} - {{ title }}</title>
</head>
<body>
    <div id="main-container">
        {{ include "partials/header.vto" }}

        <main id="main">
            <section class="main-content">
                <h1 class="page-title">{{ title |> toUpperCase() }}</h1>
                {{ content |> safe }}
            </section>

            <footer>
                {{ include "partials/footer.vto" }}
                <div class="mobile-only">
                    {{ include "partials/copyright.vto" }}
                </div>
            </footer>
        </main>
    </div>

    <style>
        :root {
            --logo: url({{ site.logo |> url }});
            --logo_mobile: url({{ site.logo_mobile |> url }});
            --logo_width: {{ site.logo_width }};
            /* Icons */
            --icon-menu: url({{ "list" |> icon("phosphor")}});
            --icon-close: url({{ "x" |> icon("phosphor") }});
            /* Socials */
            --icon-instagram: url({{ "instagram-logo:thin" |> icon("phosphor") }});
            --icon-bsky: url({{ "butterfly:thin" |> icon("phosphor") }});
            --icon-mastodon: url({{ "mastodon-logo:thin" |> icon("phosphor") }});
            --icon-youtube: url({{ "youtube-logo:thin" |> icon("phosphor") }});
            --icon-tiktok: url({{ "tiktok-logo:thin" |> icon("phosphor") }});
            --icon-bandcamp: url({{ "parallelogram:thin" |> icon("phosphor") }});
        }
    </style>
    <script src="/assets/scripts/global.js"></script>
</body>
</html>